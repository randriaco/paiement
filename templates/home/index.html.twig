{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <div class="container">

        <br>
        <h1>Lien vers le panier</h1>

        <p>Vous pouvez voir votre panier en cliquant sur le lien ci-dessous</p>

        {% if app.user %}
			<a class="btn btn-primary" href="{{ path('cart') }}">Panier</a>
			<a class="btn btn-primary" href="{{ path('app_logout') }}">Déconnexion</a>
		{% else %}
			<a class="btn btn-primary" href="{{ path('app_register') }}">Inscription</a>
			<a class="btn btn-primary" href="{{ path('app_login') }}">Connexion</a>
		{% endif %}
    
    </div>
{% endblock %}
